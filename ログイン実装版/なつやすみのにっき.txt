ぼくがやったこと
・ログイン機能
・簡単そうだったので左カラム、右上ログインボタンの切り替え実装しときました
・prifile→profile修正しときました


ぼくがきづいたこと
・profile.jspの非ログイン時の画面が必要
・adutter.htm is 何
・非ログイン時のデータベース読み込みエラーの例外処理
　(わからんタイガー状態になったら後回しにしていいです)


おまけ
・ログイン中か否かの確認

    <% //twitterオブジェクトが存在する場合(ログイン済みの場合)
        if (session.getAttribute("twitter") != null) { %>

    <% //そうでない場合
    } else { %>

    <% } %>


・ログイン中のユーザー情報の保存場所
　jsp側のuser変数に格納されてます

    <%=user != null ? user.getなんたらかんたら : null%>

　って感じでよびだしてくだちい


以上です